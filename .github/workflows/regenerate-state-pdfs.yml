name: Regenerate State PDFs

# Triggers:
#   - Every day at 07:00 UTC (03:00 ET) — covers overnight submissions
#   - Manual dispatch from Actions tab or from the /admin button
on:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:
    inputs:
      state:
        description: "Optional: single state code (e.g. MD). Leave blank for all 30+ states."
        required: false
        default: ""

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: scripts/pdf/requirements.txt

      - name: Install Python deps
        run: pip install -r scripts/pdf/requirements.txt

      - name: Install Chromium for Playwright
        run: python -m playwright install --with-deps chromium

      - name: Regenerate PDFs
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          cd scripts/pdf
          if [ -n "${{ github.event.inputs.state }}" ]; then
            echo "Regenerating single state: ${{ github.event.inputs.state }}"
            python generate_state_pdf.py --source=supabase "${{ github.event.inputs.state }}"
          else
            echo "Regenerating every state with 30+ submissions"
            python generate_state_pdf.py --source=supabase --all-30plus
          fi

      - name: Copy generated PDFs into website/public/state-reports
        run: |
          mkdir -p public/state-reports
          if [ -d scripts/pdf/outputs/state_packets_pdf ]; then
            cp -v scripts/pdf/outputs/state_packets_pdf/*.pdf public/state-reports/
          else
            echo "No PDFs produced (no states over threshold?)."
          fi

      - name: Commit and push if changed
        run: |
          git config user.name  "standwithmeg-bot"
          git config user.email "bot@standwithmeg.com"
          git add public/state-reports/*.pdf
          if git diff --staged --quiet; then
            echo "No PDF changes — nothing to commit."
            exit 0
          fi
          changed=$(git diff --staged --name-only | wc -l | tr -d ' ')
          git commit -m "chore: auto-regenerate ${changed} state PDF(s) from live Supabase data"
          git push
